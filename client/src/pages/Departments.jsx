import { useState } from 'react';
import { Link, useSearchParams } from 'react-router-dom';

import AppShell from '../components/layout/AppShell.jsx';
import useAuth from '../hooks/useAuth.js';
import useLanguage from '../hooks/useLanguage.js';
import { useDepartments, useCreateDepartment, useUpdateDepartment, useDeleteDepartment } from '../hooks/useDepartments.js';
import { useEmployees } from '../hooks/useEmployees.js';
import DataTable from '../components/common/DataTable.jsx';
import Modal from '../components/common/Modal.jsx';
import ConfirmDialog from '../components/common/ConfirmDialog.jsx';
import DepartmentForm from '../components/departments/DepartmentForm.jsx';
export default function Departments() { const [searchParams] = useSearchParams(); const { hasPermission } = useAuth(); const { t } = useLanguage(); const canManage = hasPermission('departments:manage'); const { data: departments = [], isLoading, isError, error, refetch } = useDepartments(); const { data: employeeData } = useEmployees({ page: 1, limit: 100 }); const employees = employeeData?.data || []; const createMut = useCreateDepartment(); const updateMut = useUpdateDepartment(); const deleteMut = useDeleteDepartment(); const [formOpen, setFormOpen] = useState(() => searchParams.get('create') === '1'); const [editing, setEditing] = useState(null); const [deleting, setDeleting] = useState(null); const [serverErrors, setServerErrors] = useState({}); const [formError, setFormError] = useState(''); const open = (item = null) => { setEditing(item); setServerErrors({}); setFormError(''); setFormOpen(true); }; const submit = async (payload) => { setServerErrors({}); setFormError(''); try { if (editing) await updateMut.mutateAsync({ id: editing._id, payload }); else await createMut.mutateAsync(payload); setFormOpen(false); } catch (requestError) { const response = requestError.response?.data; if (response?.errors) setServerErrors(response.errors); setFormError(response?.message || t('saveDepartmentError')); } }; const remove = async () => { try { await deleteMut.mutateAsync(deleting._id); setDeleting(null); } catch {} }; const columns = [{ key: 'name', header: t('departments'), render: (item) => <div><Link to={`/departments/${item._id}`} className="font-medium text-primary-600 hover:underline">{item.name}</Link><p className="text-xs text-gray-400">{item.code}</p></div> }, { key: 'manager', header: t('manager'), render: (item) => item.managerId ? `${item.managerId.firstName} ${item.managerId.lastName}` : '—' }, { key: 'employeeCount', header: t('employees') }, { key: 'internCount', header: t('interns') }, { key: 'status', header: t('status'), render: (item) => <Badge active={item.isActive} /> }]; if (canManage) columns.push({ key: 'actions', header: '', className: 'text-right', render: (item) => <div className="flex justify-end gap-2"><button onClick={() => open(item)} className="text-primary-600 hover:underline text-sm">{t('edit')}</button><button onClick={() => setDeleting(item)} className="text-red-600 hover:underline text-sm">{t('delete')}</button></div> }); return <AppShell><div className="mx-auto max-w-7xl px-4 py-6 sm:px-6 sm:py-8"><div className="flex items-center justify-between mb-4"><div><h2 className="text-lg font-semibold text-gray-800">{t('departmentDirectory')}</h2><p className="text-sm text-gray-500">{t('departmentsSubtitle')}</p></div>{canManage && <button onClick={() => open()} className="bg-primary-600 text-white px-4 py-2 rounded-md text-sm font-medium">{t('addDepartment')}</button>}</div><DataTable columns={columns} rows={departments} loading={isLoading} error={isError ? error : null} onRetry={refetch} emptyTitle={t('noDepartments')} emptyMessage={canManage ? t('createFirstDepartment') : t('noActiveDepartments')} /><Modal open={formOpen} onClose={() => setFormOpen(false)} title={editing ? t('editDepartment') : t('addDepartment')} size="lg">{formError && <p className="mb-4 text-sm text-red-600 bg-red-50 border border-red-200 rounded-md px-3 py-2">{formError}</p>}<DepartmentForm initial={editing} employees={employees} onSubmit={submit} onCancel={() => setFormOpen(false)} submitting={createMut.isPending || updateMut.isPending} serverErrors={serverErrors} /></Modal><ConfirmDialog open={!!deleting} onClose={() => { if (!deleteMut.isPending) setDeleting(null); }} onConfirm={remove} title={t('deleteDepartment')} message={deleting ? t('deleteDepartmentConfirm', { name: deleting.name }) : ''} confirmLabel={t('delete')} loading={deleteMut.isPending} /></div></AppShell>; }
function Badge({ active }) { const { t } = useLanguage(); return <span className={`px-2 py-0.5 rounded text-xs font-medium ${active ? 'bg-green-100 text-green-700' : 'bg-gray-100 text-gray-600'}`}>{active ? t('active') : t('inactive')}</span>; }
