import { useState } from 'react';
import { Link } from 'react-router-dom';

import AppShell from '../components/layout/AppShell.jsx';
import useAuth from '../hooks/useAuth.js';
import useLanguage from '../hooks/useLanguage.js';
import { useInternBatches, useCreateInternBatch, useUpdateInternBatch, useDeleteInternBatch } from '../hooks/useInternBatches.js';
import DataTable from '../components/common/DataTable.jsx';
import Pagination from '../components/common/Pagination.jsx';
import Modal from '../components/common/Modal.jsx';
import ConfirmDialog from '../components/common/ConfirmDialog.jsx';
import BatchForm from '../components/interns/BatchForm.jsx';
import StatusBadge from '../components/interns/StatusBadge.jsx';
export default function InternBatches() { const { hasPermission } = useAuth(); const { t, locale } = useLanguage(); const canManage = hasPermission('interns:manage'); const [page, setPage] = useState(1); const { data, isLoading, isError, error, isFetching, refetch } = useInternBatches({ page, limit: 10 }); const createMut = useCreateInternBatch(); const updateMut = useUpdateInternBatch(); const deleteMut = useDeleteInternBatch(); const [formOpen, setFormOpen] = useState(false); const [editing, setEditing] = useState(null); const [deleting, setDeleting] = useState(null); const [serverErrors, setServerErrors] = useState({}); const [formError, setFormError] = useState(''); const [uploadProgress, setUploadProgress] = useState(0); const open = (item = null) => { setEditing(item); setServerErrors({}); setFormError(''); setFormOpen(true); }; const submit = async (payload, file) => { setServerErrors({}); setFormError(''); const onUploadProgress = (event) => { if (event.total) setUploadProgress(Math.round((event.loaded / event.total) * 100)); }; try { if (editing) await updateMut.mutateAsync({ id: editing._id, payload, file, onUploadProgress }); else await createMut.mutateAsync({ payload, file, onUploadProgress }); setFormOpen(false); } catch (requestError) { const response = requestError.response?.data; if (response?.errors) setServerErrors(response.errors); setFormError(response?.message || t('saveBatchError')); } }; const remove = async () => { try { await deleteMut.mutateAsync(deleting._id); setDeleting(null); } catch {} }; const columns = [{ key: 'batch', header: t('batch'), render: (item) => <div><Link to={`/intern-batches/${item._id}`} className="font-medium text-primary-600 hover:underline">{item.code}</Link><p className="text-xs text-gray-400">{item.title}</p></div> }, { key: 'timeline', header: t('timeline'), render: (item) => `${new Date(item.startDate).toLocaleDateString(locale)} – ${new Date(item.endDate).toLocaleDateString(locale)}` }, { key: 'internCount', header: t('interns') }, { key: 'status', header: t('status'), render: (item) => <StatusBadge status={item.status} /> }]; if (canManage) columns.push({ key: 'actions', header: '', className: 'text-right', render: (item) => <div className="flex justify-end gap-2"><button onClick={() => open(item)} className="text-primary-600 hover:underline text-sm">{t('edit')}</button><button onClick={() => setDeleting(item)} className="text-red-600 hover:underline text-sm">{t('delete')}</button></div> }); return <AppShell><div className="mx-auto max-w-7xl px-4 py-6 sm:px-6 sm:py-8"><div className="flex items-center justify-between mb-4"><div><h2 className="text-lg font-semibold text-gray-800">{t('batchTimeline')}</h2><p className="text-sm text-gray-500">{t('batchesSubtitle')}</p></div>{canManage && <button onClick={() => open()} className="bg-primary-600 text-white px-4 py-2 rounded-md text-sm font-medium">{t('addBatch')}</button>}</div><DataTable columns={columns} rows={data?.data} loading={isLoading} error={isError ? error : null} onRetry={refetch} emptyTitle={t('noBatches')} emptyMessage={t('createFirstBatch')} />{data?.pagination && <Pagination {...data.pagination} onPageChange={setPage} disabled={isFetching} />}<Modal open={formOpen} onClose={() => setFormOpen(false)} title={editing ? t('editBatch') : t('addBatch')} size="lg">{formError && <p className="mb-4 text-sm text-red-600 bg-red-50 border border-red-200 rounded-md px-3 py-2">{formError}</p>}<BatchForm initial={editing} onSubmit={submit} onCancel={() => setFormOpen(false)} submitting={createMut.isPending || updateMut.isPending} uploadProgress={uploadProgress} serverErrors={serverErrors} /></Modal><ConfirmDialog open={!!deleting} onClose={() => setDeleting(null)} onConfirm={remove} title={t('deleteBatch')} message={deleting ? t('deleteBatchConfirm', { name: deleting.code }) : ''} confirmLabel={t('delete')} loading={deleteMut.isPending} /></div></AppShell>; }
