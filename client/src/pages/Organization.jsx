import { useState } from 'react';
import OrganizationChart from '../components/organization/OrganizationChart.jsx'; import { useOrganizationTree } from '../hooks/useOrganization.js'; import { useDepartments } from '../hooks/useDepartments.js'; import useLanguage from '../hooks/useLanguage.js'; import AppShell from '../components/layout/AppShell.jsx';
export default function Organization() { const { t } = useLanguage(); const [departmentId, setDepartmentId] = useState(''); const { data: departments = [] } = useDepartments(); const { data: tree, isLoading, isError, error } = useOrganizationTree(departmentId ? { department: departmentId } : {}); return <AppShell><div className="flex flex-col sm:flex-row sm:items-start sm:justify-between gap-3 mb-6"><div><p className="text-sm text-gray-500">{t('dashboard')} / {t('organizationChart')}</p><h1 className="text-2xl font-bold text-gray-800 mt-1">{t('organizationTitle')}</h1><p className="text-gray-500 mt-1">{t('organizationSubtitle')}</p></div></div>{isLoading && <div className="rounded-xl border border-gray-200 bg-white p-12 text-center text-gray-500">{t('loadingChart')}</div>}{isError && <div className="rounded-xl border border-red-200 bg-red-50 p-6 text-center text-red-700">{error?.response?.data?.message || t('chartError')}</div>}{tree && !isError && <OrganizationChart tree={tree} departments={departments} departmentId={departmentId} onDepartmentChange={setDepartmentId} />}</AppShell>; }
